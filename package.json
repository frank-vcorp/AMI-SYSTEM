{
  "name": "ami-system-monorepo",
  "version": "0.1.0",
  "description": "AMI-SYSTEM v2.0 - Monorepo con Next.js + Prisma + TypeScript",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "type-check": "tsc --version"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
